# 企微通知 & 自选监控预警重构 — 需求文档

> 状态：待核实（用户确认后进入编码 / 功能测试 / 上线部署）
> 日期：2026-07-28
> 依据：已通读 `Market.jsx` / `Wecom.jsx` / `server/lib/wecom.js` / `server/lib/dotAnalysis.js` / `server/routes/api.js` / `client/src/components/Chart.jsx` 梳理现状

---

## 1. 背景与目标

### 现状问题（已读码确认）
1. **通知分三路、截图不完整**：
   - 实时看盘页 `Market.jsx` 当前标的命中顶/底 → 纯文字通知（不带图）。
   - 服务端 `dotAnalysis.startLiveScanner` 每 3 分钟扫全部自选股 → 纯文字通知（不带图）。
   - 企微测试预览 `Wecom.jsx` → 发「当日分时图 + K线图」**两张** ECharts 截图。
   - 现有截图用 ECharts 原生 `getDataURL`，**只能截 canvas，截不了 HTML 面板**（做T决策面板、概率匹配列表都是普通 DOM）。
2. **自选监控有独立规则**：阈值 / 逐股配置入口分散，用户希望"加自选即监控"，不要额外配置。
3. 用户要求：统一触发 = **实时看盘当日分时主图的高置信顶/底信号**；通知附 **「当日分时 + 做T决策面板 + 当前分时形态概率匹配」三模块一张完整截图**；自选监控默认全量、删逐股规则、保留全局顶/底开关；每次提示写日志。

### 目标
- 顶/底信号（与图中标记同一套）作为唯一触发条件。
- 通知截图 = 三模块一张完整图（替换原分时 + K线两张）。
- 自选监控：默认全部自选股纳入顶/底预警，删逐股规则，保留全局顶/底总开关。
- 每次触发提示都落日志。

---

## 2. 现状梳理（关键事实）

| 通知来源 | 触发 | 是否带图 | 图内容 |
|---|---|---|---|
| 实时看盘页 `Market.jsx` | 当前查看标的出现高置信顶/底 | ❌ 纯文字 | 无 |
| 自选监控扫描 `dotAnalysis.js` | 每 3 分钟扫全部自选股 | ❌ 纯文字 | 无 |
| 企微测试预览 `Wecom.jsx` | 手动点测试 | ✅ 2 张 | 当日分时图 + K线图 |

- **信号已统一**：图中顶/底标记 `tbMarks`（`Market.jsx:129` 调 `api.dotTopsBottoms`）与服务端扫描器用的都是后端 `detectTopsBottoms`，仅筛 `confidence==='high'`。
- **截图技术限制**：ECharts `getDataURL` 只能截 canvas；要"三模块一张完整图"必须改用 **DOM 整体截图（html2canvas，纯浏览器端、无需 Chrome）**，把包住三个模块的容器整体截为一图。
- **概率匹配数据**：`🧩 当前分时形态概率匹配` 走 `/api/dot/replay?pattern=1`（盘中实时逐根匹配），与图中同源。
- **日志能力**：`wecom.js` 已有 `appendNotifyLog`（落 `global.db` 的 `notify_log`，含 code/name/type/time/ok/reason），`/api/notify` 调用即落库。

---

## 3. 需求范围与改动

### 3.1 统一触发条件
- 触发信号 = 后端 `detectTopsBottoms` 的 **高置信（high）顶 / 底**，与实时看盘主图标记同一套。
- 去重键：`code | date | type | time`（或 index），当日只推一次，次日清零。

### 3.2 截图：三模块一张完整图（替换原分时 + K线两张）
- 引入 `html2canvas`（加至 `package.json`，经 esbuild 打包）。
- **新增可复用组件 `<StockSnapshot code=... />`**：按实时看盘页布局渲染
  - 左（约 66%）：**当日分时图**（含顶/底标记、成交量、日内T指标副图）
  - 右（约 34%）：**做T决策面板**（振幅/昨收/换手/均价/现价/最高/最低/今开/买卖区）+ **当前分时形态概率匹配**（最佳匹配率 + 各形态概率分布）
  - 整体包在一个带 `ref` 的容器，供截图。
- 截图：`html2canvas(container, { backgroundColor:'#fff', pixelRatio: 1.5 })` → dataURL → 随通知作为**单张图**发送。
- 文字内容保持：标的名称/代码、信号类型（顶/底）、价格、均价、时间。

### 3.3 自选监控重构
- **删除**逐股 / 阈值的预警规则配置（如 `dotAnalysis` 阈值类规则 UI、Wecom 页逐股开关等入口）。
- **默认**：所有加入自选监控的股票自动纳入顶/底预警，无需额外配置。
- **保留**全局「顶部信号通知 / 底部信号通知」总开关（`notifyTop` / `notifyBottom`）。

### 3.4 企微测试预览改造（`Wecom.jsx`）
- 原「📉 当日分时（将截图）」「🕯 K线（将截图）」两个面板 → 替换为单个 `<StockSnapshot>` 预览（同一布局、同一标的可切换）。
- 测试发送：`html2canvas` 截该预览 → **单张图**随通知发送；按钮保留「发送底部测试 / 发送顶部测试」。
- 文案同步更新（去掉"含分时与K线截图"等旧描述）。

### 3.5 日志
- 每次触发提示均写日志，复用 `wecom.appendNotifyLog`（落 `global.db.notify_log`）。
- 前端监控引擎与测试预览都经 `POST /api/notify` 落库，保证"每次提示都做日志记录"。

---

## 4. 架构设计

### 4.1 前端监控引擎（主路径，带截图）
实时看盘页加载时读取自选列表（`gstore.getWatchlist`）：
- **当前查看标的**：复用现有 `tbMarks` 推送逻辑（`Market.jsx:382`），命中时对**屏幕上已渲染的三模块容器**直接 `html2canvas` 截图并发送。
- **其余自选股（后台监控）**：定时（建议每 1–3 分钟）对每只自选股调 `/api/dot/tops-bottoms` 检测新高置信顶/底；命中后在**隐藏容器（off-screen，渲染但不可见）**挂载 `<StockSnapshot code=命中股>`，待其渲染完成 → `html2canvas` 截图 → `POST /api/notify` 发送 → 卸载。**仅命中时才渲染**，控制开销。
- **去重**：前端维护已推送键集合（建议同步 `localStorage`，防页面刷新重复推送）。

### 4.2 服务端扫描器（`dotAnalysis.startLiveScanner`）处理 —— 需你确认
- **推荐方案**：降级为**纯文字兜底**——页面关闭时仍发顶/底信号文字通知（不带截图），避免漏信号；带截图的主路径由前端负责。
- **备选方案**：直接删除服务端扫描器（页面关闭则不再有提醒）。
- （请在文档核实时选定其一）

### 4.3 截图技术注意
- `html2canvas` 捕获含 ECharts `<canvas>` 的容器一般可行（会 drawImage 画布）；设白底、适中 `pixelRatio`。
- 截图在前端浏览器完成，无服务端渲染问题（本机无 Chrome 不影响，html2canvas 不需 Chrome）。

---

## 5. 涉及文件

| 文件 | 改动 |
|---|---|
| `client/src/components/StockSnapshot.jsx` | **新增**：三模块组合 + ref 容器 |
| `client/src/pages/Market.jsx` | 当前标的推送加截图；新增后台自选股监控循环 |
| `client/src/pages/Wecom.jsx` | 测试预览改为三模块单图 |
| `client/src/api.js` | 如需新增快照/概率数据接口 |
| `server/lib/wecom.js` | `sendNotify` 已支持 `images`（单图）；确认发送逻辑 |
| `server/lib/dotAnalysis.js` | `startLiveScanner` 降级/删除；清理阈值规则 |
| `server/routes/api.js` | `/api/notify` 已支持 `images`；按需补接口 |
| `package.json` | 增加 `html2canvas` 依赖 |

---

## 6. 验收标准

- [ ] 实时看盘页当前标的出现高置信顶/底 → 收到企微通知，附「当日分时 + 决策面板 + 概率匹配」**一张完整截图**。
- [ ] 自选股中任一只（非当前查看）出现顶/底 → 前端后台监控命中并附**该标的**三模块截图。
- [ ] 企微测试预览只显示一个三模块预览，「发送测试」附带单张截图。
- [ ] 全局顶/底开关可分别控制；逐股规则配置入口已移除。
- [ ] 每次提示在 `notify_log` 有记录。
- [ ] 通知截图**不再含 K线图**。

---

## 7. 风险与注意
- `html2canvas` 对 ECharts canvas 的兼容性需在浏览器真机自测（本机无 Chrome，但 html2canvas 在浏览器端运行，不依赖 Chrome）。
- 后台监控多自选股时接口频率：建议轮询间隔 ≥ 1 分钟、命中才渲染，避免卡顿。
- 页面关闭后无截图提醒（见 §4.2，依赖兜底方案）。
