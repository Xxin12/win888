# 复制本文件为 .env 后按需修改（docker-compose 默认读取 .env）

# 服务监听端口（容器内，compose 已映射到宿主机同端口）
PORT=5178

# 时区：A股收盘补齐调度依赖本地时间，务必保持上海
TZ=Asia/Shanghai

# ===== 可选：TDX 网关（历史数据深度回溯）=====
# 留空则使用腾讯/新浪/东财公开数据源（默认，容器内无需 Python）。
# 若自建 TDX 网关并暴露到宿主机，可填下方地址让容器内服务连过去：
# TDX_ENDPOINT=http://host.docker.internal:10000
# TDX_TOKEN=

# ===== 企微通知配置 =====
# 应用内企微通知读取 data/wecom-config.json，无需在此配置；
# 若希望用环境变量注入 webhook key，可在此添加（需服务端支持读取，当前为占位）：
# WECOM_KEY=
